# Documentation for idiots

## 1. This is the database info


```json lines
DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.mysql',
        'NAME': 'ticksy_cmdb',
        'USER': 'ticksy_dev',-
        'PASSWORD': 'dev',
        'HOST': 'localhost',
        'PORT': '',
    }
}
```